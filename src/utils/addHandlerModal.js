/* import WeeklyWeather from "../js/components/WeeklyWeather/WeeklyWeather";

export const addHeadlerModal = (e) => {
  const modal = document.querySelector("#modal");
  document.querySelectorAll(".weather_card").forEach((item) => {
    const url = item.getAttribute("data-name");
    item.addEventListener("click", () => {
      WeeklyWeather(url);
      modal.classList.add("show");
    });
  });
}; */
